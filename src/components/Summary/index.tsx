import React from 'react';
import { Box } from '@mui/system';

// ? p po x 30px po y 30px
export default function Summary() {
  return (
    <Box
      sx={{
        width: '50%',
        height: 'auto',
        padding: 30,
      }}
    ></Box>
  );
}
